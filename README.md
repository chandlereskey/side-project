Dash application with a few tabs including
a home page, todo list page, sample interactive graph page, 
and face detection page. 

Both web app and sql server (storing graph data and face embeddings) 
are hosted im Azure.
